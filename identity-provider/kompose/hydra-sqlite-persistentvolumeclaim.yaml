apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: hydra-sqlite
  name: hydra-sqlite
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
