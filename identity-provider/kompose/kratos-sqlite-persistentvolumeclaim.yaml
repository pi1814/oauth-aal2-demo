apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: kratos-sqlite
  name: kratos-sqlite
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
